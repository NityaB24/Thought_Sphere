<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 p-10 text-white">
            <nav class="flex justify-end gap-10 border-2 border-zinc-800 p-2 rounded-xl mb-4 text-1xl ">
                <a href="/profile" class=" ">Profile</a>
                <a class="px-3 bg-red-500 rounded-md  text-sm inline-block" href="/logout">Logout</a>
            </nav>
        <div class="flex items-start gap-3">
            <h1 class="text-3xl tracking-tighter mb-3 ">Welcome to <span class="text-[#cdea66]"> Thought-Sphere </span></h1>
        </div>
        
    
        <div class="posts mt-20">
            <h3 class="text-zinc-500">All Posts</h3>
            <div class="postcontainer mt-5 ">
                <% post.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 ml-[30vw] mb-5 rounded-md border-2 border-zinc-800 bg-zinc-800 p-4">
                        <h4 class="text-[#cdea66] mb-2">@<%=post.user.username%></h4>
                        <p class="text-sm tracking-tight"><%= post.content%></p>
                        <small class="mt-5 inline-block"><%= post.likes.length%> Likes</small>
                        <div class="btns flex gap-4 mt-2">
                            <a class="text-red-500" href="/likes/<%=post._id%>">
                                <%= post.likes.indexOf(post.user.email) ===-1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-zinc-500" href="/edit/<%=post._id%>">Edit</a>
                        </div>
                    </div>
               <% }) %>

            </div>
        </div>
    </div>
    
</body>
</html>